/**
 * Copy to env.local.ts and add your Sentry DSN (never commit env.local.ts).
 *
 * Create a free project at https://sentry.io → Settings → Client Keys (DSN)
 */
export const envLocal = {
  SENTRY_DSN: '',
};
