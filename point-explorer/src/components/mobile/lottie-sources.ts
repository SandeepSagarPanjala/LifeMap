/** Same JSON files as the React Native app (`assets/lottie/`). */
import driveCar from '@lifemap/assets/lottie/drive-car.json';
import visitRelax from '@lifemap/assets/lottie/visit-relax.json';

export const VISIT_RELAX_LOTTIE = visitRelax;
export const DRIVE_CAR_LOTTIE = driveCar;
