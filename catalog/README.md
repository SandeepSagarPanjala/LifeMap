# Activity template catalog

Portable **YAML** activity definitions for LifeMap. The app fetches this file (or paste YAML) and installs validated templates into the local catalog.

- No executable code — declarative fields only
- Update this file to ship new templates without an app release
- App catalog URL: configured in `src/lib/activities/activity-catalog.ts`
